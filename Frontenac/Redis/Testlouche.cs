namespace Frontenac.Redis
{
    public static class Testlouche
    {
        public static string RedisElementToString(object obj)
        {
            var str = obj.ToString();
            return str;
        }
    }
}